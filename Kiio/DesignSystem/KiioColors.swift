import SwiftUI

enum KiioColors {
    static let background = Color(red: 250 / 255, green: 250 / 255, blue: 248 / 255)
    static let surface = Color.white
    static let text = Color(red: 26 / 255, green: 26 / 255, blue: 26 / 255)
    static let secondaryText = Color(red: 98 / 255, green: 95 / 255, blue: 90 / 255)
    static let mutedText = Color(red: 116 / 255, green: 111 / 255, blue: 104 / 255)
    static let accent = Color(red: 112 / 255, green: 95 / 255, blue: 77 / 255)
    static let accentSoft = Color(red: 112 / 255, green: 95 / 255, blue: 77 / 255).opacity(0.14)
    static let border = Color(red: 216 / 255, green: 206 / 255, blue: 192 / 255)
    static let disabledFill = Color(red: 232 / 255, green: 227 / 255, blue: 220 / 255)
    static let success = Color(red: 35 / 255, green: 125 / 255, blue: 72 / 255)
    static let warning = Color(red: 150 / 255, green: 95 / 255, blue: 0 / 255)
    static let info = Color(red: 0 / 255, green: 102 / 255, blue: 204 / 255)
    static let danger = Color(red: 164 / 255, green: 61 / 255, blue: 61 / 255)
}
